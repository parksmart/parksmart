<div class="form-group">
    <label for="${name}" class="col-sm-2 control-label">${label}</label>
    <div class="col-sm-2">
        <input type="number" name="${name}" min="0" class="form-control" value="${value}" placeholder="${name}"/>
    </div>
</div>