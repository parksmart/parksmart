<div class="form-group">
    <label for="${name}" class="col-sm-3 control-label">${label}</label>
    <div class="col-sm-9">
        <g:textField name="${name}" onfocus="${onfocus}" class="form-control" value="${value}" placeholder="${name}"/>
    </div>
</div>